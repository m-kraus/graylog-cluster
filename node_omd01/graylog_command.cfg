define command {
  command_name                   check_elasticsearch
  command_line                   /bin/check_elasticsearch -H $HOSTADDRESS$ -m $ARG1$
}

define command {
  command_name                   check_graylog_server
  command_line                   $USER2$/check_graylog2_server -U $ARG1$
}
